{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "@powershell if (Test-Path dist) { Remove-Item -Recurse -Force dist } && cd ../phonebook-frontend && npm run build && @powershell Copy-Item dist -Recurse ../phonebook-backend-include-part3",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"initial code for the backend and the frontend \" && git push",
    "dev": "node --watch index.js",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dot": "^1.1.3",
    "dotenv": "^17.2.2",
    "env": "^0.0.2",
    "express": "^5.1.0",
    "mongoose": "^8.18.1",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@stylistic/eslint-plugin-js": "^4.4.1",
    "eslint": "^9.35.0"
  }
}
